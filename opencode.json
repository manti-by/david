{
  "$schema": "https://opencode.ai/config.json",
  "lsp": {
    "pyright": {
      "disabled": true
    },
    "ty": {
      "command": ["uvx", "ty", "server"],
      "extensions": [".py", ".pyi"]
    }
  },
  "command": {
    "linear": {
      "template": "Retrieve a linear issue with the highest priority from the TODO column and implement it. Follow instructions in the AGENTS.md file for Git and Linear workflows.",
      "description": "Implement linear ticket",
      "agent": "build"
    }
  }
}
